export const TILE_SIZE = 32;
export const PLAYER_SIZE = 24;
export const PLAYER_SPEED = 160;
export const RUN_MULTIPLIER = 1.8;
export const RUN_THRESHOLD_MS = 2000;
export const NPC_SIZE = 24;

export enum TileType {
  FLOOR = 0,
  WALL = 1,
}
