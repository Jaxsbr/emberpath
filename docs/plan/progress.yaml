# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.
#
# Field ownership — each field has exactly one owner responsible for writes.
# Other commands may READ any field but must never WRITE fields they don't own.
#
#   Owner key:
#     init        = build-loop-init (scaffolding, one-time creation)
#     orchestrator = build-loop (dispatcher: resume, skip, abort, pause)
#     iterator    = build-loop-iterate (continuous loop: start, iterate)
#     retro       = phase-retro (retrospective gate)
#
# See MANUAL.md § State ownership for the full ownership table.

config:
  verify: "npx tsc --noEmit && npm run build"
  max_tasks_per_phase: 200
  quality_check_suppressions: []
  build_loop_version:
    init_version: 12
    builds: [12, 13]

state:
  # --- Lifecycle (owner: iterator) ---
  phase: fog-marsh-dead-end
  phase_complete: false
  status: running

  # --- Task orchestration (owner: iterator) ---
  task_number: 11
  next_task: "[phase] Docs reconciliation — create docs/plan/fog-marsh-dead-end-manual-verify.md (covering Threshold + Exit closure + Visual closure + Escape attempts + Reset/save-resume + Tampered flags subsections per the spec's user guidance), then update AGENTS.md: (a) Behavior rules gains a 'Conditional exits and decorations' entry covering shared condition field semantics + flag-change re-render contract + no-scene-restart rule + 'Trigger flag side-effects' (setFlags + incrementFlags); (b) File ownership rows updated for data/areas/types.ts (added TriggerDefinition.setFlags, TriggerDefinition.incrementFlags, DecorationDefinition.condition), data/areas/fog-marsh.ts (threshold + escape triggers + conditional exit/decorations), scenes/GameScene.ts (decoration condition evaluation + flag-change re-render hook + collision flip), triggers/flags.ts (flag-change subscriber API), systems/triggerZone.ts (consumes setFlags + incrementFlags); (c) Directory layout adds docs/plan/fog-marsh-dead-end-manual-verify.md entry."
  last_result: pass
  consecutive_fails: 0

  # --- Review (owner: iterator) ---
  review_stage: null
  review_complete: false
  pr_number: null

  # --- Retrospective (owner: retro) ---
  retro_complete: false

  # --- Tracking (owner: iterator) ---
  completed_stories: [US-66, US-67, US-68, US-69]
