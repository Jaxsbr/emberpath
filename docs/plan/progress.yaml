# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.
#
# Field ownership — each field has exactly one owner responsible for writes.
# Other commands may READ any field but must never WRITE fields they don't own.
#
#   Owner key:
#     init        = build-loop-init (scaffolding, one-time creation)
#     orchestrator = build-loop (dispatcher: resume, skip, abort, pause)
#     iterator    = build-loop-iterate (continuous loop: start, iterate)
#     retro       = phase-retro (retrospective gate)
#
# See MANUAL.md § State ownership for the full ownership table.

config:
  verify: "npx tsc --noEmit && npm run build"
  max_tasks_per_phase: 200
  quality_check_suppressions: []
  build_loop_version:
    init_version: 12
    builds: [12, 13]

state:
  # --- Lifecycle (owner: iterator) ---
  phase: briar-wilds
  phase_complete: false
  status: running

  # --- Task orchestration (owner: iterator) ---
  task_number: 9
  next_task: "[US-100] Operator-driven PixelLab Wang tileset generation for briar-wilds. Fire mcp__pixellab__create_topdown_tileset (PERSONAL account — Learning EP-05 — verify mcp__pixellab__ has remaining budget; team server NOT used). Subject: weathered grey-green briar floor blending into twisted dead-bramble thorn, kid-safe never-horror, sepia-first storybook palette, no hope-gold accents on terrain. View: high top-down. Outline: selective. Shading: basic. Detail: medium. Tile size 16x16. Primary terrain 'briar-floor', secondary 'briar-thorn'. Compose prompt via composeArtPrompt(subject, mood). On completion: commit assets/tilesets/briar-wilds-floor-thorn/tilemap.png + tilemap.json provenance; update TILESETS atlasKey to 'tileset-briar-wilds-floor-thorn'; render labeled-atlas preview via tools/atlas-preview.py and verify each Wang mask before commit lands (Learning EP-03)."
  last_result: pass
  consecutive_fails: 0
  phase_classification: cross-cutting

  # --- Review (owner: iterator) ---
  review_stage: null
  review_complete: false
  pr_number: null

  # --- Retrospective (owner: retro) ---
  retro_complete: false

  # --- Tracking (owner: iterator) ---
  completed_stories: []
