# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.
#
# Field ownership — each field has exactly one owner responsible for writes.
# Other commands may READ any field but must never WRITE fields they don't own.
#
#   Owner key:
#     init        = build-loop-init (scaffolding, one-time creation)
#     orchestrator = build-loop (dispatcher: resume, skip, abort, pause)
#     iterator    = build-loop-iterate (continuous loop: start, iterate)
#     retro       = phase-retro (retrospective gate)
#
# See MANUAL.md § State ownership for the full ownership table.

config:
  verify: "npx tsc --noEmit && npm run build"
  max_tasks_per_phase: 200
  quality_check_suppressions: []
  build_loop_version:
    init_version: 12
    builds: [12, 13]

state:
  # --- Lifecycle (owner: iterator) ---
  phase: tile-architecture
  phase_complete: false
  status: running

  # --- Task orchestration (owner: iterator) ---
  task_number: 6
  next_task: "[US-93] Implement src/maps/wang.ts — resolveWangFrame(tilesetId, terrainIds: [TL, TR, BR, BL]) function with FNV-1a hash-pick, JSDoc pinning clockwise corner convention, one-time-per-mask warn Set for absent-mask fallback. Update src/maps/tilesets.ts: add WangTilesetDefinition shape (cornerMaskTable + fallbackFrames), register tiny-town and tiny-dungeon as degenerate Wang tilesets ('0000' = FLOOR frames, '1111' = WALL frames, all 14 other masks omitted), keep legacy TilesetDefinition + resolveFrame alive for now (consumer migration follows in US-94). Add src/maps/atlasPreview.ts (TS port — generates a 4×4 labelled grid PNG via canvas)."
  last_result: pass
  consecutive_fails: 0
  phase_classification: cross-cutting

  # --- Review (owner: iterator) ---
  review_stage: null
  review_complete: false
  pr_number: null

  # --- Retrospective (owner: retro) ---
  retro_complete: false

  # --- Tracking (owner: iterator) ---
  completed_stories: []
