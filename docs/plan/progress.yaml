# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.
#
# Field ownership — each field has exactly one owner responsible for writes.
# Other commands may READ any field but must never WRITE fields they don't own.
#
#   Owner key:
#     init        = build-loop-init (scaffolding, one-time creation)
#     orchestrator = build-loop (dispatcher: resume, skip, abort, pause)
#     iterator    = build-loop-iterate (continuous loop: start, iterate)
#     retro       = phase-retro (retrospective gate)
#
# See MANUAL.md § State ownership for the full ownership table.

config:
  verify: "npx tsc --noEmit && npm run build"
  max_tasks_per_phase: 200
  quality_check_suppressions: []
  build_loop_version:
    init_version: 12
    builds: [12, 13]

state:
  # --- Lifecycle (owner: iterator) ---
  phase: world-legibility
  phase_complete: false
  status: running

  # --- Task orchestration (owner: iterator) ---
  task_number: 8
  next_task: "[US-60] Implement Fog Marsh redesign — single coherent commit authoring the decorations array in src/data/areas/fog-marsh.ts to match the schematic in docs/product/phases/world-legibility.md. Tiny Dungeon atlas (Read /Users/jacobusbrink/Jaxs/projects/emberpath/assets/tilesets/tiny-dungeon/tilemap.png to pick frames). Topology per schematic: (a) wet-ground frame for most FLOOR cells in the marsh interior — pick a moss / damp / green-grey tiny-dungeon frame distinct from the existing tan dungeon-floor frames 48-51 (substitute documented in inline comment if no perfect match); (b) dry-path overlay tracing a connected route from south entry (row 21-22 cols 13-16) through the interior to the Marsh Hermit's stoop adjacent to the ruin's door — use a stone-step / boardwalk frame distinct from the wet-ground; (c) ruin corner — 7×7 brick-walled enclosure at NE corner (rows 3-9 cols 21-27 per schematic) using brick frames 0-2 (existing WALL frames retained for visual coherence) plus door frame 22 at (24, 9); (d) ≥8 reed/cattail decorations scattered through wet-ground cells, NEVER on the dry path; (e) full one-edge impassable wet/reed-wall — rows 0-1 + 22-23 left-side full impassable border (water/deep-marsh frame, substitute if not present); (f) Marsh Hermit spawn moved onto the dry path adjacent to ruin door (24, 10) — keep him on the path; (g) Whispering Stones trigger renders adjacent to or on the path — relocate from current pos to (13, 16) on/just-off the dry path; (h) preserve all existing fog-marsh trigger ids (start-thought, ashen-isle-vision, room-echo) actionRefs, conditions; (i) update entryPoint of fog-marsh exit-back-to-ashen so player lands on dock-adjacent tile (24, 4) on the new ashen layout; (j) update Ashen Isle's entryPoint into fog-marsh from current (2, 12) to a dry-path tile near the south entry (14, 21). Use the same builder helpers pattern as ashen-isle.ts (rect/hline/vline/fencePerimeter/rectVariants) extracted into the file or inlined. Verify game tsc+build + editor build pass."
  last_result: pass
  consecutive_fails: 0

  # --- Review (owner: iterator) ---
  review_stage: null
  review_complete: false
  pr_number: null

  # --- Retrospective (owner: retro) ---
  retro_complete: false

  # --- Tracking (owner: iterator) ---
  completed_stories: [US-58, US-59, US-60]
