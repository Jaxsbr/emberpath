# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.
#
# Field ownership — each field has exactly one owner responsible for writes.
# Other commands may READ any field but must never WRITE fields they don't own.
#
#   Owner key:
#     init        = build-loop-init (scaffolding, one-time creation)
#     orchestrator = build-loop (dispatcher: resume, skip, abort, pause)
#     iterator    = build-loop-iterate (continuous loop: start, iterate)
#     retro       = phase-retro (retrospective gate)
#
# See MANUAL.md § State ownership for the full ownership table.

config:
  verify: "npx tsc --noEmit && npm run build"
  max_tasks_per_phase: 200
  quality_check_suppressions: []
  build_loop_version:
    init_version: 12
    builds: [12, 13]

state:
  # --- Lifecycle (owner: iterator) ---
  phase: fog-marsh-dead-end
  phase_complete: false
  status: running

  # --- Task orchestration (owner: iterator) ---
  task_number: 6
  next_task: "[US-67] Implement: (a) src/triggers/flags.ts — add lightweight subscriber API: type FlagChangeListener = (name: string, value: FlagValue|undefined) => void; module-level Map<string, Set<FlagChangeListener>> listeners; export onFlagChange(name, cb): () => void returns unsubscribe; private notifyChange(name, oldValue, newValue) bails when oldValue === newValue (Learning EP-01: once per change, not per setFlag call), then iterates listeners.get(name) and invokes each; setFlag captures oldValue then notifyChange; incrementFlag captures oldValue then notifyChange; resetAllFlags after the existing wipe iterates listeners.keys() and notifies each with (name, undefined). (b) src/data/areas/fog-marsh.ts — set condition: 'marsh_trapped == false' on the existing fog-to-ashen exit. (c) src/scenes/GameScene.ts — import onFlagChange; add private field marshTrappedUnsubscribe?: () => void; add method applyMarshTrappedState() that computes trapped = getFlag('marsh_trapped') === true and mutates this.area.map[22][13..16] to WALL when trapped, FLOOR when not (only when this.area.id === 'fog-marsh' to scope the mutation; defensive guard against running on Ashen Isle). Call applyMarshTrappedState() ONCE in create() right after this.area is assigned (so a save-resumed Continue with marsh_trapped already true restores the trap state on Fog Marsh load). Subscribe in create(): this.marshTrappedUnsubscribe = onFlagChange('marsh_trapped', () => this.applyMarshTrappedState()) — only when this.area.id === 'fog-marsh' (other areas don't need the listener). In cleanupResize, call this.marshTrappedUnsubscribe?.() and clear the field. Verify: npx tsc --noEmit && npm run build pass; cd tools/editor && npm run build passes (flowRenderer already renders exit.condition as a label per line 191 — no editor change needed). Story Completion Gate: 7 of 8 [US-67] structural criteria become source-readable post-commit; 1 runtime criterion (regression: marsh_trapped==false fresh game still transitions to Ashen Isle) is source-readable from the unchanged checkExitZones path + the new condition string."
  last_result: pass
  consecutive_fails: 0

  # --- Review (owner: iterator) ---
  review_stage: null
  review_complete: false
  pr_number: null

  # --- Retrospective (owner: retro) ---
  retro_complete: false

  # --- Tracking (owner: iterator) ---
  completed_stories: []
