# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.
#
# Field ownership — each field has exactly one owner responsible for writes.
# Other commands may READ any field but must never WRITE fields they don't own.
#
#   Owner key:
#     init        = build-loop-init (scaffolding, one-time creation)
#     orchestrator = build-loop (dispatcher: resume, skip, abort, pause)
#     iterator    = build-loop-iterate (continuous loop: start, iterate)
#     retro       = phase-retro (retrospective gate)
#
# See MANUAL.md § State ownership for the full ownership table.

config:
  verify: "npx tsc --noEmit && npm run build"
  max_tasks_per_phase: 200
  quality_check_suppressions: []
  build_loop_version:
    init_version: 12
    builds: [12, 13]

state:
  # --- Lifecycle (owner: iterator) ---
  phase: world-legibility
  phase_complete: false
  status: running

  # --- Task orchestration (owner: iterator) ---
  task_number: 7
  next_task: "[US-59] Implement Ashen Isle redesign — single coherent commit authoring the decorations array in src/data/areas/ashen-isle.ts to match the schematic in docs/product/phases/world-legibility.md. Substitute cliff/stone-edge for water (Tiny Town atlas does not include water frames; documented in inline comment + US-61 vocabulary doc). Use semantic helper arrays at the top of the file (built via Array.from + spread) for: (a) cliff coast — rows 0-1 all cols with frame 120 (stone-block); (b) main vertical path — col 24-25 rows 4-37 with frame 15 (dirt patch); (c) west branch path — row 20 cols 10-23 frame 15; (d) east branch path — row 22 cols 26-39 frame 15; (e) player's cottage — roof at rows 12-13 cols 8-12 frame 60, wall-front rows 14-15 cols 8-12 frame 84, door at row 15 col 10 frame 96; (f) player's fence — perimeter at rows 11-19 cols 5-14 with gate gap at row 19 col 9 frame 128; (g) Old Man's cottage — same shape at rows 24-28 cols 36-40, door row 28 col 40, fence rows 23-31 cols 36-45 with gate gap at row 19 col 40 — actually align to schematic; (h) dock at exit — row 2 cols 23-26 frame 47; (i) ~8 scattered decorations using existing prop-known frames (bush 28/29, flower 26, sign 27, stone 13, tree 6/7/10) at points beyond the path; (j) verify Old Man npc spawn (currently col 13 row 12) is moved or the cottage repositioned so that npc spawn is on a path tile adjacent to cottage door — adjust npc.col/row to match schematic's (40, 29) one south of door at (40, 28); (k) update playerSpawn from (2, 2) to schematic's (9, 20) on the west-branch path adjacent to player's gate. EXIT zone: keep base EXIT frame '15' OR overlay a dock decoration on the EXIT tiles at (24, 2) — choose decoration overlay so EXIT zone visually shows wooden dock. Verify game tsc+build + editor build pass; fade-in shows player + path + at least one landmark in viewport. AGENTS.md ashen-isle file-ownership row already mentions decorations from US-58 work — no doc edit needed."
  last_result: pass
  consecutive_fails: 0

  # --- Review (owner: iterator) ---
  review_stage: null
  review_complete: false
  pr_number: null

  # --- Retrospective (owner: retro) ---
  retro_complete: false

  # --- Tracking (owner: iterator) ---
  completed_stories: [US-58, US-59]
