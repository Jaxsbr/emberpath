# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.
#
# Field ownership — each field has exactly one owner responsible for writes.
# Other commands may READ any field but must never WRITE fields they don't own.
#
#   Owner key:
#     init        = build-loop-init (scaffolding, one-time creation)
#     orchestrator = build-loop (dispatcher: resume, skip, abort, pause)
#     iterator    = build-loop-iterate (continuous loop: start, iterate)
#     retro       = phase-retro (retrospective gate)
#
# See MANUAL.md § State ownership for the full ownership table.

config:
  verify: "npx tsc --noEmit && npm run build"
  max_tasks_per_phase: 200
  quality_check_suppressions: []
  build_loop_version:
    init_version: 12
    builds: [12, 13]

state:
  # --- Lifecycle (owner: iterator) ---
  phase: fog-marsh-dead-end
  phase_complete: false
  status: running

  # --- Task orchestration (owner: iterator) ---
  task_number: 2
  next_task: "[US-66] Investigate: confirm TriggerZoneSystem.tryFire is the correct injection point for new optional setFlags + incrementFlags fields on TriggerDefinition. Produce concrete diff plan for (a) types.ts adding TriggerDefinition.setFlags?: Record<string, string|number|boolean> + incrementFlags?: string[] (additive — no breaking change); (b) triggerZone.ts iterating both fields on fire AFTER condition check + AFTER one-shot setFlag bookkeeping but BEFORE the type-dispatch switch (so the flag side-effect lands before the thought/dialogue/story callback so any consumer that reads marsh_trapped during the same callback frame sees true); (c) fog-marsh.ts adding trigger id 'marsh-deepens' at (col 14, row 5), width 1, height 2, type 'thought', actionRef 'The fog rolls in behind me. The path is gone.', repeatable false, setFlags { marsh_trapped: true }. Verify the existing fog-entry-thought trigger continues to fire without setFlags side-effects (variant baseline)"
  last_result: pass
  consecutive_fails: 0

  # --- Review (owner: iterator) ---
  review_stage: null
  review_complete: false
  pr_number: null

  # --- Retrospective (owner: retro) ---
  retro_complete: false

  # --- Tracking (owner: iterator) ---
  completed_stories: []
