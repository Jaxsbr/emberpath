# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.
#
# Field ownership — each field has exactly one owner responsible for writes.
# Other commands may READ any field but must never WRITE fields they don't own.
#
#   Owner key:
#     init        = build-loop-init (scaffolding, one-time creation)
#     orchestrator = build-loop (dispatcher: resume, skip, abort, pause)
#     iterator    = build-loop-iterate (continuous loop: start, iterate)
#     retro       = phase-retro (retrospective gate)
#
# See MANUAL.md § State ownership for the full ownership table.

config:
  verify: "npx tsc --noEmit && npm run build"
  max_tasks_per_phase: 200
  quality_check_suppressions: []
  build_loop_version:
    init_version: 12
    builds: [12, 13]

state:
  # --- Lifecycle (owner: iterator) ---
  phase: homecoming-light
  phase_complete: false
  status: running

  # --- Task orchestration (owner: iterator) ---
  task_number: 5
  next_task: "[US-85] Implement EmberShareSystem foundation — per the diff plan from task 4: (1) NEW src/systems/emberShare.ts exports `EmberShareSystem` class with EMBER_PULSE_DURATION_MS=600, EMBER_COLOR=0xf2c878, EMBER_PULSE_DEPTH=5.5 named constants; constructor(scene); startPulse(playerSprite, targetNpcSprite, onComplete) creates a Phaser.GameObjects.Arc at player position growing radius and traveling to npc, alpha-tweens to 0 over duration, on tween complete fires onComplete + clears in-flight refs; instance fields pulseObject + pulseTween reset at top of setup-relevant entry method (Learning EP-02); scene.events.on('shutdown', …) cancels in-flight tween and destroys pulseObject; uiCam.ignore on the Arc. (2) GameScene: add private `sharingInProgress = false`; instantiate this.emberShare in create(); add `if (this.sharingInProgress) return;` in update() AFTER spawnInProgress and BEFORE the dialogue branch (so it suppresses movement + npcInteraction + triggerZone via the existing chain); add setSharingInProgress(boolean) helper. (3) src/systems/dialogue.ts: extend DialogueChoice type with optional `firePulseTarget?: string` (NPC id); selectChoice, when firePulseTarget is set, fires onChoiceCallback BUT defers the showNode(nextNode) — the GameScene handler is responsible for advancing dialogue when the pulse onComplete fires (new dialogueSystem.advanceToNextNode(choiceIndex) helper, OR pass resume callback into onChoiceCallback). (4) GameScene setOnChoice handler: if choice.firePulseTarget is set, resolve it via npcSpritesById (Map populated in renderNpcs), set sharingInProgress=true, call this.emberShare.startPulse(this.player, npcSprite, onComplete). onComplete: setFlag(`npc_warmed_${choice.firePulseTarget}`, true) (or use choice.setFlags as the source of truth — TBD during implement, prefer choice.setFlags since spec says the flag IS the warming marker), set sharingInProgress=false, advance dialogue. (5) Verify build still green. NO ASSET DEP — Wren/Driftwood gens still processing; integrate them in iter 6+."
  last_result: pass
  consecutive_fails: 0

  # --- Review (owner: iterator) ---
  review_stage: null
  review_complete: false
  pr_number: null

  # --- Retrospective (owner: retro) ---
  retro_complete: false

  # --- Tracking (owner: iterator) ---
  completed_stories: []
