# Build Loop — Progress State
# Schema: config + state read every iteration. Validate with yamllint.
#
# Field ownership — each field has exactly one owner responsible for writes.
# Other commands may READ any field but must never WRITE fields they don't own.
#
#   Owner key:
#     init        = build-loop-init (scaffolding, one-time creation)
#     orchestrator = build-loop (dispatcher: resume, skip, abort, pause)
#     iterator    = build-loop-iterate (continuous loop: start, iterate)
#     retro       = phase-retro (retrospective gate)
#
# See MANUAL.md § State ownership for the full ownership table.

config:
  verify: "npx tsc --noEmit && npm run build"
  max_tasks_per_phase: 200
  quality_check_suppressions: []
  build_loop_version:
    init_version: 12
    builds: [12, 13]

state:
  # --- Lifecycle (owner: iterator) ---
  phase: npc-portraits
  phase_complete: false
  status: running

  # --- Task orchestration (owner: iterator) ---
  task_number: 11
  next_task: "[US-57] Implement DialogueSystem portrait rendering in src/systems/dialogue.ts. Additions: (a) module constants PORTRAIT_DESIGN_SIZE=96 and PORTRAIT_MOBILE_WIDTH_FRACTION=0.22 near other UI constants. (b) imports — NPC_PORTRAITS, hasNpcPortrait from '../systems/npcSprites'. (c) fields — portraitImage: Phaser.GameObjects.Image | null = null; currentPortraitId: string | null = null. (d) helpers — resolvePortraitId(node) returns node.portraitId ?? this.script?.portraitId ?? null; applyPortrait(id) with four-branch guard (null→null no-op, null→id create+position+filter+depth+scrollFactor+ignoreOnMainCamera, id→same no-op, id→other setTexture swap+reposition, error path on unknown id or missing texture: console.error + destroy + null state); repositionPortrait() sets origin (0,1), position (s(BOX_PADDING), boxY), displaySize min(s(PORTRAIT_DESIGN_SIZE), canvasWidth*PORTRAIT_MOBILE_WIDTH_FRACTION) square; repositionSpeakerLabel() — speaker x = s(BOX_PADDING) + (portraitImage ? portraitImage.displayWidth + s(8) : 0). (e) wiring — call applyPortrait(resolvePortraitId(node)) at the top of showNode AFTER currentNode assignment; replace direct speaker setPosition in redrawBox with a call to repositionSpeakerLabel after portrait reposition; close() destroys portraitImage and nulls both portrait fields; clearChoices() left untouched (portrait persists across choice transitions). Verify (npx tsc --noEmit && npm run build) must pass. Do NOT update AGENTS.md or editor in this task — those are queued separately."
  last_result: pass
  consecutive_fails: 0

  # --- Review (owner: iterator) ---
  review_stage: null
  review_complete: false
  pr_number: null

  # --- Retrospective (owner: retro) ---
  retro_complete: false

  # --- Tracking (owner: iterator) ---
  completed_stories: []
