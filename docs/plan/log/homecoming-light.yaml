phase: "homecoming-light"
started: "2026-04-29T07:36:05Z"
entries:
  - task: 1
    time: "2026-04-29T07:45:00Z"
    type: investigate
    story: phase
    description: "Branch B — initial state survey for homecoming-light. Verify clean: npx tsc --noEmit + npm run build pass on build/homecoming-light at commit 264d7f3 (post-start commit). Source layout confirmed: existing systems (lighting, dialogue, npcBehavior, npcInteraction, conditions, triggerZone, desaturationPipeline) all in src/systems/; areas in src/data/areas/{ashen-isle,fog-marsh,registry,types}.ts; npcSprites + npcPortraits registries co-located in src/systems/npcSprites.ts. Existing NPC asset directories: heron (keeper-rescue), marsh-hermit (npc-portraits), old-man (foundation/world-legibility). New for this phase: wren (PixelLab generation needed, longest async blocker → US-82) and driftwood (PixelLab generation needed → US-83). Phase classification: SERVER/CROSS-CUTTING — touches GameScene (sharingInProgress mutual exclusion + flag-change subscribers), ashen-isle.ts (3 new dialogue scripts, 1 new NPC + extended Old Man scripts, 1 new trigger), npcSprites/npcPortraits registries, new EmberShareSystem, lighting (re-registration on warm), desaturation pipeline (effective strength derived value), alpha-gates (force-eval on warm). Investigate-first mandate applies. Story sequencing rationale: kick off Wren+Driftwood PixelLab generation FIRST since they are async blockers (~minutes); while assets generate, build US-85 (EmberShareSystem keystone) → US-86 (desaturation derived value, no asset dep) → US-84 (Old Man dialogue extensions, no asset dep). When Wren assets land, fold in US-82; when Driftwood assets land, fold in US-83. Queued first concrete task: investigate Wren PixelLab kickoff specifics so iteration 2's Branch A can fire the create_character / animate_character batch."
    result: pass
    notes: phase classification = SERVER/CROSS-CUTTING; investigate-first mandate active; sequencing puts long-async PixelLab gen first
    tokens: ~3500
  - task: 2
    time: "2026-04-29T07:55:00Z"
    type: investigate
    story: US-82
    description: "Branch A — executed [US-82] Investigate: Wren PixelLab generation kickoff. Findings: heron asset shape is 8 idle dirs / 8 walk dirs / 8 static / portrait — matches the spec's expected layout for wren (assets/npc/wren/{idle,walk}/{dir}/frame_NNN.png + static/{dir}.png + portrait.png). Heron prompt.md confirms PRO mode + size=60 (84×84 canvas / 1.4) + view='low top-down' + body_type=humanoid (anthropomorphic bipedal animal) was the pattern; matched it. Personal pixellab MCP (mcp__pixellab__*) is now connected after a fresh registration in this session. Description authored from spec ('young, chirpy, hopeful', 'small brown bird, distinct silhouette') + heron-style structure (anthropomorphic bipedal + watercolor storybook + cosmetic marker scarf). Fired mcp__pixellab__create_character: name='Wren', mode='pro', body_type='humanoid', size=60, view='low top-down'. Returned character_id=27194dce-f37c-4363-8ca8-bee96373051f, status=processing, cost=20 generations. ETA 3-5 min. No code change in this iteration (asset generation is server-side async); verify trivially still pass; no test-baseline update needed. Story Completion Gate: 0/10 US-82 criteria met (assets not yet on disk; this is a kickoff). Queued next task: kick off Driftwood (US-83) so both characters generate in parallel; once both queued, the next iteration can shift to US-85 code work while we wait."
    result: pass
    notes: "Wren character_id=27194dce-f37c-4363-8ca8-bee96373051f (pro mode, 20 gens). Heron-style pattern confirmed. No code change yet."
    tokens: ~3000
  - task: 3
    time: "2026-04-29T08:00:00Z"
    type: implement
    story: US-83
    description: "Branch A — executed [US-83] Driftwood PixelLab kickoff. Mirrored Wren params: mode='pro', body_type='humanoid', size=60, view='low top-down'. Description authored from spec ('charming smooth-talking worldly traveler', sea otter species per spec) — anthropomorphic bipedal sea otter with chocolate-brown fur, sea-green sailor's scarf + leather travel satchel as cosmetic markers (distinct from Wren's dusty rose scarf and Heron's earth-tone hooded cloak). Fired mcp__pixellab__create_character. Returned character_id=bba49891-4a1b-4186-9f72-c32fa2b8e804, status=processing, cost=20 generations. ETA 3-5 min. Both Wren + Driftwood now generating in parallel. No code change yet (asset gen is server-side async); verify trivially pass; no test-baseline update. Story Completion Gate: 0/8 US-83 criteria met (assets not yet on disk). Queued next: US-85 investigate (EmberShareSystem foundation integration points) so code work proceeds while assets process."
    result: pass
    notes: "Driftwood character_id=bba49891-4a1b-4186-9f72-c32fa2b8e804 (pro, 20 gens). Both NPCs queued; total spend so far ~40 gens."
    tokens: ~2000
  - task: 4
    time: "2026-04-29T08:10:00Z"
    type: investigate
    story: US-85
    description: "Branch A — executed [US-85] Investigate: EmberShareSystem integration points. Findings: (a) GameScene.ts:98+117+406-411 — existing world-walk early-return pattern. transitionInProgress (98) and spawnInProgress (117) early-return at 406+409 BEFORE the dialogueSystem.isActive branch (411). npcInteraction.update (461), triggerZone.update (467), checkExitZones (468), animationSystem.update (430), maybeAutosave are ALL inside the post-early-return body. ONE new `if (this.sharingInProgress) return;` between line 409 and 411 covers movement + npcInteraction + triggerZone suppression. (b) DialogueSystem.selectChoice (dialogue.ts:512-525): fires onChoiceCallback(choice) at line 516-518 then advances to nextNode at 519-523. To pause dialogue during pulse, extend DialogueChoice with optional firePulseTarget?: string; selectChoice, when set, calls onChoiceCallback but DOES NOT advance — relies on the callback to resume via a new dialogueSystem helper (advanceToNextNode or similar). (c) GameScene.ts:334 already has a setOnChoice handler that processes choice.setFlags — extend it to handle firePulseTarget by resolving via npcSpritesById Map + invoking emberShare.startPulse. (d) LightingSystem.registerLight (lighting.ts:158-167): idempotent overwrite confirmed (findIndex + replace, Learning #63). Re-call with the same NPC id at higher radius/intensity = brightens. (e) Alpha-gate force-eval: GameScene.maybeUpdateAlphaGates(true) (line 690) — already invoked from has_ember_mark onFlagChange (line 365) on US-77's atomic flip pattern. Mirror this from a new onFlagChange('npc_warmed_<id>') subscriber. (f) npcSpritesById Map: existing GameScene field tracking NPC id -> sprite; Learning #57+US-71 reference. Use it to resolve firePulseTarget into a target sprite. (g) DESIGN NOTE — choice.setFlags vs onComplete-set flag: spec says onComplete sets the flag. Cleaner to set the flag in onComplete (so the flag flip is exactly when the pulse lands, not when the choice is made) — this means choice.firePulseTarget DOES NOT use choice.setFlags, the flag is set programmatically in the warming-success handler. Output: queued task 5 with a precise diff plan covering 5 file edits (new emberShare.ts + GameScene + dialogue.ts type extension + GameScene setOnChoice handler + verify). No code change in this iteration."
    result: pass
    notes: "Diff plan recorded; implement task #5 queued. Wren+Driftwood still processing in background."
    tokens: ~5500
  - task: 5
    time: "2026-04-29T08:25:00Z"
    type: implement
    story: US-85
    description: "Branch A — implemented [US-85] EmberShareSystem foundation per task 4's diff plan. Five files: (1) NEW src/systems/emberShare.ts — EmberShareSystem class with EMBER_COLOR=0xf2c878, EMBER_PULSE_DURATION_MS=600, EMBER_PULSE_DEPTH=5.5 named constants; constructor(scene) initializes pulseObject+pulseTween nulls and registers scene.events.once('shutdown') handler that stops the tween + destroys the Arc; startPulse(player, npc, onComplete) resets in-flight refs at top, creates a Phaser.GameObjects.Arc at player position with EMBER_PULSE_END_RADIUS, scale-tweens from EMBER_PULSE_START_RADIUS/END (4/12) to 1 while translating to npc.x/y and alpha-tweening to 0 over 600ms (Sine.easeOut), uiCam.ignore on the Arc, on tween complete destroys the Arc + nulls refs + fires onComplete. (2) src/data/areas/types.ts — DialogueChoice gains optional firePulseTarget?: string; choice with this set is the 'Share warmth' authoring marker. (3) src/systems/dialogue.ts — selectChoice fires onChoiceCallback then EARLY-RETURNS when choice.firePulseTarget is set (defers auto-advance); new public advanceAfterPulse(choice) — finds nextNode and calls showNode (or close if missing) — the GameScene handler invokes this from the pulse onComplete. (4) src/scenes/GameScene.ts — imports EmberShareSystem; private sharingInProgress + emberShare!: EmberShareSystem fields; create() resets sharingInProgress and instantiates emberShare AFTER setupCamera (UI camera must exist before the pulse Arc is uiCam.ignore'd); update() gains `if (this.sharingInProgress) return;` between the spawnInProgress check and the dialogue branch — single early-return covers movement + npcInteraction + triggerZone + exit-zone checks via the existing chain; setOnChoice handler resolves choice.firePulseTarget via npcSpritesById, sets sharingInProgress=true, calls emberShare.startPulse(this.player, npcSprite, () => { setFlag(`npc_warmed_${targetId}`, true); this.sharingInProgress=false; this.dialogueSystem.advanceAfterPulse(choice); }) — defensive console.warn + fall-through to advanceAfterPulse if sprite missing. (5) AGENTS.md — new systems/emberShare.ts row in File ownership table; new 'Ember sharing (US-85)' Behavior rule explaining the choice → defer-advance → pulse → flag flip → advance flow; sharingInProgress added to the zone-level mutual exclusion item alongside transitionInProgress and spawnInProgress. Verify: npx tsc --noEmit + npm run build pass. Bundle 1546.19 → 1556.97 kB (+10.78 kB; module count 30 → 34). Test-baseline.txt promoted to current. Golden Principles: Mechanical truth ✓ (pulse + future light/decoration changes are felt, not text); Restoration targets persons ✓ (firePulseTarget is an NPC id, not arbitrary); Loop-invariant EP-01 ✓ (zero per-frame work — pulse setup is event-driven); Restart hygiene EP-02 ✓ (refs reset + shutdown handler). Quality checks: no-silent-pass N/A (no tests); no-bare-except N/A (no try/catch); error-path-coverage ✓ (missing-sprite path logs + falls through); agents-consistency ✓ (table + behavior rule + zone-level mutex updated). Scope check: 5 files all under PROJECT_ROOT ✓. Secrets: clean. Story Completion Gate: 8/16 US-85 criteria met (foundation done; pending criteria — light brightening / alpha-gate force-eval subscribers, manual-verify doc, per-NPC variant verification — wire alongside Wren/Old Man warming flags during US-82/US-84 implements). NO ticked criteria yet since US-85 isn't fully complete. Commit: feat: ember-share foundation — EmberShareSystem + sharingInProgress mutual exclusion + DialogueChoice.firePulseTarget (US-85). Polled both PixelLab characters: Wren (116×116) + Driftwood (100×100) rotations ready, no animations yet."
    result: pass
    notes: "US-85 foundation 8/16 criteria met. EmberShareSystem ships with full restart hygiene + scene-shutdown teardown. Wren+Driftwood rotations ready — animations + portraits queued next."
    tokens: ~9500
  - task: 6
    time: "2026-04-29T08:35:00Z"
    type: implement
    story: phase
    description: "Branch A — partial execution of [US-82+US-83] PixelLab animation + portrait kickoff. Hit PixelLab's 8-concurrent-job cap: Wren breathing-idle (8 directional jobs) consumed all 8 slots; the other 5 calls (Wren walking-4-frames, Driftwood breathing-idle, Driftwood walking-4-frames, Wren portrait, Driftwood portrait) all returned 'Insufficient job slots' / 'Maximum concurrent background jobs reached'. Will retry the remaining 5 jobs in a serialised pipeline (1 batch at a time, ~2-4 min each) over subsequent iterations. Total spend so far: 40 (Wren+Driftwood characters in Pro mode) + 8 (Wren breathing-idle) = 48 generations. Remaining for Wren+Driftwood asset gen: 24 anim jobs + 2 portrait jobs = 26 generations. No code change in this iteration; polling Wren in next iteration."
    result: pass
    notes: "PixelLab job cap = 8 concurrent. Will pipeline remaining batches between code iterations to avoid wall-time blocking."
    tokens: ~2500
