phase: area-system
started: 2026-04-09T22:15:26Z
entries:
  - task: 1
    type: investigate
    story: US-14
    description: "Investigated codebase. All 49 criteria unmet. 4 global data files (npcs, triggers, dialogues, story-scenes) + worldMap + MAP_COLS/MAP_ROWS in constants. GameScene, collision, npcInteraction, triggerZone all import globals directly. Queued US-14 investigate for AreaDefinition/ExitDefinition planning."
    result: pass
    tokens: ~5000
  - task: 2
    type: investigate
    story: US-14
    description: "Deep investigation of existing types. NpcDefinition, TriggerDefinition/TriggerType, DialogueScript/Node/Choice, StorySceneDefinition/Beat all exist. NPC_SIZE and getNpcBounds are utilities to keep accessible. collision.ts/npcInteraction.ts/triggerZone.ts all import globals directly. Migration plan: create src/data/areas/ with types.ts (AreaDefinition, ExitDefinition), ashen-isle.ts (area data), registry.ts (lookup). Queued implement task."
    result: pass
  - task: 3
    type: implement
    story: US-14
    description: "Created src/data/areas/types.ts (AreaDefinition, ExitDefinition, re-exported NpcDefinition, TriggerDefinition, DialogueScript, StorySceneDefinition + TriggerType with 'exit'), src/data/areas/ashen-isle.ts (50x38 map, Old Man NPC, 3 triggers, dialogue, story scene, spawn (2,2), visual config), src/data/areas/registry.ts (getArea lookup, getDefaultAreaId). All types pass tsc strict. ✓ US-14 complete"
    result: pass
    tokens: ~15000
