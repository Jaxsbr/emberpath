phase: "keeper-rescue"
started: "2026-04-26T09:30:00Z"
entries:
  - task: 1
    time: "2026-04-26T09:35:00Z"
    type: investigate
    story: phase
    description: "Branch B — phase start. Read AGENTS.md fully (current; reflects fog-marsh-dead-end shipping incl. Conditional exits and decorations + Trigger flag side-effects behavior rules; existing onFlagChange API on triggers/flags.ts; existing applyMarshTrappedState + updateConditionalDecorations + conditionalDecorations subscriber on GameScene). Read docs/plan/phase-goal.md (US-70/71/72/73; 4 stories; full done-when transposed). Read docs/product/phases/keeper-rescue.md spec for story bodies + design rationale + drift reconciliation notes (AND-only condition parser, heron assets-on-disk, DialogueNode.setFlags, generalised flag-change subscriber). Confirmed heron source assets at ~/Jaxs/assets/emberpath/npc/heron/: idle 32 PNGs, walk 32 PNGs, static 8 PNGs, portrait.png, prompt.md (also a fireball/ folder out-of-scope this phase). Confirmed src/systems/npcSprites.ts shape: NPC_SPRITES exposes 'marsh-hermit' + 'old-man' { idleFrameCount: 4, walkFrameCount: 4 }; NPC_PORTRAITS exposes those two with file: 'portrait.png' and filter 'nearest'/'linear'. Confirmed assets/npc/ has marsh-hermit + old-man; need to add heron/. Verify (npx tsc --noEmit && npm run build) passes — baseline established (dist/index.html + 1542 KB bundle). Phase classified server/schema/cross-cutting: US-71 adds NpcDefinition.spawnCondition + GameScene generalised flag-change subscriber + conditional spawn render path; US-72 adds DialogueNode.setFlags + DialogueScript.endStoryScene to data/areas/types.ts and showNode side-effect in dialogue system; US-73 adds depth-5.5 ember overlay to GameScene update loop. Investigate-first mandated for each implement task. Story priority: US-70 first (asset/registry foundation; US-71's heron sprite requires it, US-72's portrait requires it). Per investigate-first mandate, queued task 2 as [US-70] investigate (registry + asset-copy plan)."
    result: pass
    notes: phase classified server/schema/cross-cutting; investigate-first enforced; baseline verify passes; heron source assets confirmed
    tokens: ~14000
  - task: 2
    time: "2026-04-26T09:40:00Z"
    type: investigate
    story: US-70
    description: "Branch A — executed queued [US-70] investigate. Source review: src/systems/npcSprites.ts (54 lines) — NPC_SPRITES + NPC_PORTRAITS are flat record literals; adding 'heron' is two parallel inline entries (no helper changes; no schema changes). The portrait `linear` filter mirrors 'old-man' (painterly portrait downscale; matches the spec criterion for the Keeper as 'allegorically other'). vite.config.ts line 5: `publicDir: 'assets'` — Vite copies the entire assets/ tree into dist/ on build, so once the heron folder is on disk under assets/npc/heron/, `dist/assets/npc/heron/` materialises automatically with no build script changes. Asset copy plan: `cp -R /Users/jacobusbrink/Jaxs/assets/emberpath/npc/heron/{idle,walk,static,portrait.png,prompt.md} <repo>/assets/npc/heron/` — explicitly enumerate sub-paths to exclude `fireball/` (out-of-scope this phase per spec). Existing assets/npc/marsh-hermit + assets/npc/old-man tree match this layout (idle: 8 dirs × 4 frames = 32; walk: 8 dirs × 4 frames = 32; static: 8 single-frame PNGs at root; portrait.png at root). Diff plan for next iteration's implement: (1) `mkdir -p assets/npc/heron` then `cp -R` from source. (2) Add the two registry entries between the existing 'marsh-hermit' and 'old-man' rows in NPC_SPRITES + NPC_PORTRAITS. (3) Verify: `npx tsc --noEmit && npm run build` then `find dist/assets/npc/heron -type f | wc -l` to confirm ≥ 73 files (32 idle + 32 walk + 8 static + portrait.png + prompt.md = 74). Story Completion Gate after implement: 5 of 5 [US-70] criteria met by source/dist read."
    result: pass
    notes: investigation complete; concrete cp + registry diff plan recorded; one-concern enforced (US-70 is a single-task implement)
    tokens: ~3500
  - task: 3
    time: "2026-04-26T09:50:00Z"
    type: implement
    story: US-70
    description: "Branch A — implemented US-70 per the diff plan from task 2. (1) cp -R idle/ walk/ static/ portrait.png prompt.md from ~/Jaxs/assets/emberpath/npc/heron/ to assets/npc/heron/ (74 files; explicit sub-paths exclude fireball/ per spec out-of-scope). (2) src/systems/npcSprites.ts: added 'heron' row to NPC_SPRITES with { idleFrameCount: 4, walkFrameCount: 4 } between 'old-man' and the closing brace; added 'heron' row to NPC_PORTRAITS with { file: 'portrait.png', filter: 'linear' } following the same pattern as 'old-man' (painterly portrait, downscale-quality matters). Verify: npx tsc --noEmit pass; npm run build pass (1542 KB bundle, identical bytes to baseline modulo build hash). Production bundle: dist/npc/heron/ contains 74 files (matches expected ≥ 73). NOTE: spec criterion path 'dist/assets/npc/heron/' is a typo — Vite publicDir: 'assets' copies assets/ contents directly to dist/ root, so the actual location is dist/npc/heron/. Phase-goal.md criterion text updated inline to dist/npc/heron/ with a note about the spec drift. Golden Principles Gate: NPC sprite rendering registry-driven ✓ (single registry edit, no GameScene code touched), no-silent-breaking-changes ✓ (additive registry rows; existing 'marsh-hermit' + 'old-man' rows unchanged), loop-invariant EP-01 ✓ (no per-frame work added). Quality checks: no-silent-pass N/A, no-bare-except N/A, error-path-coverage ✓ (existing fallback rectangle warning preserved for unknown sprite ids), agents-consistency: AGENTS.md row updates for systems/npcSprites.ts deferred to phase-level reconciliation [phase] criteria. Regression check: SKIP — first implementation task of phase, no prior test-baseline.txt; current snapshot promoted to baseline post-commit. Scope check: assets/npc/heron/ + src/systems/npcSprites.ts all under PROJECT_ROOT ✓. Secrets scan: clean (PNG + markdown). Story Completion Gate: ALL 5 [US-70] criteria met. Ticked checkboxes in phase-goal.md (with the dist/npc/heron/ path correction). Added US-70 to completed_stories. Story milestone commit 'feat(US-70): add heron NPC sprite + portrait registry entries' committed (sha 2fe573e)."
    result: pass
    notes: US-70 complete; story milestone commit 2fe573e; test-baseline.txt promoted; spec path drift noted (dist/npc/heron/ not dist/assets/npc/heron/)
    tokens: ~5500
  - task: 4
    time: "2026-04-26T09:55:00Z"
    type: investigate
    story: phase
    description: "Branch B — picking next task. US-70 shipped; story milestone 2fe573e. Unmet criteria: US-71 (12 + class baseline + idempotency + error path + AGENTS.md row + named constants), US-72 (9 + error path + 2 regression), US-73 (12 incl. AGENTS.md depth + behavior rules), phase-level (full-arc manual verify, save/resume parity, reset parity, keeper one-shot guard, manual-verify checklist, atlas verification, build pass, AGENTS.md, deploy verify), 2 safety criteria. Priority: US-71 next — it provides the spawn mechanism US-72's dialogue depends on (player must be able to interact with the Keeper before US-72's keeper-intro fires). US-72 then closes the rescue (flag flip + story scene); US-73 closes the loop (path re-opens via the existing Phase 1 mechanism + ember overlay). Cached server/schema/cross-cutting classification (task 1) — investigate-first mandated. GameScene context (lines 142-148): existing onFlagChange('marsh_trapped') subscriber pre-existed for US-67's collision flip + US-68's decoration swap; the new generalised subscription is ADDITIVE — preserved verbatim per spec. Existing unsubscribe pattern: marshTrappedUnsubscribe + cleanupResize. Queued [US-71] investigate to design the generalised subscriber API + the spawn render path + the input-suspend mechanism."
    result: pass
    notes: US-71 next; investigate-first enforced; subscriber generalisation API to be designed in task 5
    tokens: ~3500
