phase: "keeper-rescue"
started: "2026-04-26T09:30:00Z"
entries:
  - task: 1
    time: "2026-04-26T09:35:00Z"
    type: investigate
    story: phase
    description: "Branch B — phase start. Read AGENTS.md fully (current; reflects fog-marsh-dead-end shipping incl. Conditional exits and decorations + Trigger flag side-effects behavior rules; existing onFlagChange API on triggers/flags.ts; existing applyMarshTrappedState + updateConditionalDecorations + conditionalDecorations subscriber on GameScene). Read docs/plan/phase-goal.md (US-70/71/72/73; 4 stories; full done-when transposed). Read docs/product/phases/keeper-rescue.md spec for story bodies + design rationale + drift reconciliation notes (AND-only condition parser, heron assets-on-disk, DialogueNode.setFlags, generalised flag-change subscriber). Confirmed heron source assets at ~/Jaxs/assets/emberpath/npc/heron/: idle 32 PNGs, walk 32 PNGs, static 8 PNGs, portrait.png, prompt.md (also a fireball/ folder out-of-scope this phase). Confirmed src/systems/npcSprites.ts shape: NPC_SPRITES exposes 'marsh-hermit' + 'old-man' { idleFrameCount: 4, walkFrameCount: 4 }; NPC_PORTRAITS exposes those two with file: 'portrait.png' and filter 'nearest'/'linear'. Confirmed assets/npc/ has marsh-hermit + old-man; need to add heron/. Verify (npx tsc --noEmit && npm run build) passes — baseline established (dist/index.html + 1542 KB bundle). Phase classified server/schema/cross-cutting: US-71 adds NpcDefinition.spawnCondition + GameScene generalised flag-change subscriber + conditional spawn render path; US-72 adds DialogueNode.setFlags + DialogueScript.endStoryScene to data/areas/types.ts and showNode side-effect in dialogue system; US-73 adds depth-5.5 ember overlay to GameScene update loop. Investigate-first mandated for each implement task. Story priority: US-70 first (asset/registry foundation; US-71's heron sprite requires it, US-72's portrait requires it). Per investigate-first mandate, queued task 2 as [US-70] investigate (registry + asset-copy plan)."
    result: pass
    notes: phase classified server/schema/cross-cutting; investigate-first enforced; baseline verify passes; heron source assets confirmed
    tokens: ~14000
  - task: 2
    time: "2026-04-26T09:40:00Z"
    type: investigate
    story: US-70
    description: "Branch A — executed queued [US-70] investigate. Source review: src/systems/npcSprites.ts (54 lines) — NPC_SPRITES + NPC_PORTRAITS are flat record literals; adding 'heron' is two parallel inline entries (no helper changes; no schema changes). The portrait `linear` filter mirrors 'old-man' (painterly portrait downscale; matches the spec criterion for the Keeper as 'allegorically other'). vite.config.ts line 5: `publicDir: 'assets'` — Vite copies the entire assets/ tree into dist/ on build, so once the heron folder is on disk under assets/npc/heron/, `dist/assets/npc/heron/` materialises automatically with no build script changes. Asset copy plan: `cp -R /Users/jacobusbrink/Jaxs/assets/emberpath/npc/heron/{idle,walk,static,portrait.png,prompt.md} <repo>/assets/npc/heron/` — explicitly enumerate sub-paths to exclude `fireball/` (out-of-scope this phase per spec). Existing assets/npc/marsh-hermit + assets/npc/old-man tree match this layout (idle: 8 dirs × 4 frames = 32; walk: 8 dirs × 4 frames = 32; static: 8 single-frame PNGs at root; portrait.png at root). Diff plan for next iteration's implement: (1) `mkdir -p assets/npc/heron` then `cp -R` from source. (2) Add the two registry entries between the existing 'marsh-hermit' and 'old-man' rows in NPC_SPRITES + NPC_PORTRAITS. (3) Verify: `npx tsc --noEmit && npm run build` then `find dist/assets/npc/heron -type f | wc -l` to confirm ≥ 73 files (32 idle + 32 walk + 8 static + portrait.png + prompt.md = 74). Story Completion Gate after implement: 5 of 5 [US-70] criteria met by source/dist read."
    result: pass
    notes: investigation complete; concrete cp + registry diff plan recorded; one-concern enforced (US-70 is a single-task implement)
    tokens: ~3500
  - task: 3
    time: "2026-04-26T09:50:00Z"
    type: implement
    story: US-70
    description: "Branch A — implemented US-70 per the diff plan from task 2. (1) cp -R idle/ walk/ static/ portrait.png prompt.md from ~/Jaxs/assets/emberpath/npc/heron/ to assets/npc/heron/ (74 files; explicit sub-paths exclude fireball/ per spec out-of-scope). (2) src/systems/npcSprites.ts: added 'heron' row to NPC_SPRITES with { idleFrameCount: 4, walkFrameCount: 4 } between 'old-man' and the closing brace; added 'heron' row to NPC_PORTRAITS with { file: 'portrait.png', filter: 'linear' } following the same pattern as 'old-man' (painterly portrait, downscale-quality matters). Verify: npx tsc --noEmit pass; npm run build pass (1542 KB bundle, identical bytes to baseline modulo build hash). Production bundle: dist/npc/heron/ contains 74 files (matches expected ≥ 73). NOTE: spec criterion path 'dist/assets/npc/heron/' is a typo — Vite publicDir: 'assets' copies assets/ contents directly to dist/ root, so the actual location is dist/npc/heron/. Phase-goal.md criterion text updated inline to dist/npc/heron/ with a note about the spec drift. Golden Principles Gate: NPC sprite rendering registry-driven ✓ (single registry edit, no GameScene code touched), no-silent-breaking-changes ✓ (additive registry rows; existing 'marsh-hermit' + 'old-man' rows unchanged), loop-invariant EP-01 ✓ (no per-frame work added). Quality checks: no-silent-pass N/A, no-bare-except N/A, error-path-coverage ✓ (existing fallback rectangle warning preserved for unknown sprite ids), agents-consistency: AGENTS.md row updates for systems/npcSprites.ts deferred to phase-level reconciliation [phase] criteria. Regression check: SKIP — first implementation task of phase, no prior test-baseline.txt; current snapshot promoted to baseline post-commit. Scope check: assets/npc/heron/ + src/systems/npcSprites.ts all under PROJECT_ROOT ✓. Secrets scan: clean (PNG + markdown). Story Completion Gate: ALL 5 [US-70] criteria met. Ticked checkboxes in phase-goal.md (with the dist/npc/heron/ path correction). Added US-70 to completed_stories. Story milestone commit 'feat(US-70): add heron NPC sprite + portrait registry entries' committed (sha 2fe573e)."
    result: pass
    notes: US-70 complete; story milestone commit 2fe573e; test-baseline.txt promoted; spec path drift noted (dist/npc/heron/ not dist/assets/npc/heron/)
    tokens: ~5500
  - task: 4
    time: "2026-04-26T09:55:00Z"
    type: investigate
    story: phase
    description: "Branch B — picking next task. US-70 shipped; story milestone 2fe573e. Unmet criteria: US-71 (12 + class baseline + idempotency + error path + AGENTS.md row + named constants), US-72 (9 + error path + 2 regression), US-73 (12 incl. AGENTS.md depth + behavior rules), phase-level (full-arc manual verify, save/resume parity, reset parity, keeper one-shot guard, manual-verify checklist, atlas verification, build pass, AGENTS.md, deploy verify), 2 safety criteria. Priority: US-71 next — it provides the spawn mechanism US-72's dialogue depends on (player must be able to interact with the Keeper before US-72's keeper-intro fires). US-72 then closes the rescue (flag flip + story scene); US-73 closes the loop (path re-opens via the existing Phase 1 mechanism + ember overlay). Cached server/schema/cross-cutting classification (task 1) — investigate-first mandated. GameScene context (lines 142-148): existing onFlagChange('marsh_trapped') subscriber pre-existed for US-67's collision flip + US-68's decoration swap; the new generalised subscription is ADDITIVE — preserved verbatim per spec. Existing unsubscribe pattern: marshTrappedUnsubscribe + cleanupResize. Queued [US-71] investigate to design the generalised subscriber API + the spawn render path + the input-suspend mechanism."
    result: pass
    notes: US-71 next; investigate-first enforced; subscriber generalisation API to be designed in task 5
    tokens: ~3500
  - task: 5
    time: "2026-04-26T10:05:00Z"
    type: investigate
    story: US-71
    description: "Branch A — executed queued [US-71] investigate. Source review: GameScene.ts (727 lines): renderNpcs (line 463) iterates area.npcs creating sprite per entry — adding spawnCondition gate is a single if-check before sprite create; existing onFlagChange('marsh_trapped') subscriber lines 142-148 (US-67/68 'one shared mechanism') is preserved verbatim — additive, not replaced; cleanupResize line 594+ already unsub marshTrappedUnsubscribe pattern is the template for spawnConditionUnsubscribes[]. NpcBehaviorSystem (line 47): builds runtimes Map at constructor — needs new registerNpc(def, sprite) method to add a Keeper post-construction; spawnTile/position/state/facingDirection seed identical to constructor body. NpcInteractionSystem stores npcs as reference (line 22 'this.npcs = npcs'); pushing to the same array auto-propagates so no registerNpc needed there. Collision in moveWithCollision uses npcLivePositions from NpcBehavior so once registered the Keeper bounding box collides automatically. systems/conditions.ts: evaluateCondition splits on /\\s+AND\\s+/, then each clause matches /^(\\S+)\\s*(==|!=|>=|>|<=|<)\\s*(.+)$/ — flag-name regex /\\b([a-z_][a-z0-9_]*)\\s*(==|!=|>=|>|<=|<)/gi extracts every flag name from the condition for per-flag subscription. Phaser tween: this.tweens.add({ targets: sprite, alpha: { from: 0, to: 1 }, duration: KEEPER_FADE_DURATION_MS }) — standard property tween. Input suspend: mirror existing transitionInProgress pattern — new spawnInProgress boolean checked at top of update(), cleared via this.time.delayedCall(KEEPER_INPUT_SUSPEND_MS, ...). One-shot guard for the phase-level criterion: extend spawnCondition with 'AND keeper_met == false' (third clause) — Reset wipes all flags so condition becomes false anyway; the regex parse subscribes to keeper_met too so when US-72 sets keeper_met=true the subscriber re-evaluates and the idempotency guard ('skip if already in npcSpritesById') prevents double-spawn. Diff plan finalised — see queued next_task."
    result: pass
    notes: subscriber generalisation API designed; NpcBehavior.registerNpc added; Keeper one-shot via 'keeper_met == false' clause; idempotency via npcSpritesById Map check
    tokens: ~7000
  - task: 6
    time: "2026-04-26T10:25:00Z"
    type: implement
    story: US-71
    description: "Branch A — implemented US-71 per the diff plan from task 5. (1) src/data/areas/types.ts: NpcDefinition gains optional spawnCondition?: string with inline doc-comment about the gate semantics + flag-change re-render contract. (2) src/systems/npcBehavior.ts: added registerNpc(def, sprite) — idempotent, mirrors the constructor seed shape exactly. (3) src/scenes/GameScene.ts: KEEPER_FADE_DURATION_MS = 500 and KEEPER_INPUT_SUSPEND_MS = 1000 declared at top alongside FADE_DURATION + SAVE_THROTTLE_MS; new private fields spawnConditionUnsubscribes[], spawnInProgress, activeNpcs; create() now builds activeNpcs by filtering area.npcs through evaluateCondition (NPCs without spawnCondition pass through unchanged); subscribeToConditionalSpawns() parses every conditional NPC's spawnCondition for flag names via /\\b([a-z_][a-z0-9_]*)\\s*(==|!=|>=|>|<=|<)/gi, dedupes, and registers one onFlagChange listener per unique flag (Keeper subscribes to marsh_trapped, escape_attempts, keeper_met); evaluateConditionalSpawns() walks area.npcs, spawns any conditional NPC whose condition is now true and is not yet in npcSpritesById (idempotent), pushes to activeNpcs, calls npcBehavior.registerNpc for the sprite path (rectangle fallback skipped — behavior runtime requires a Phaser.GameObjects.Sprite), tweens alpha 0->1 over KEEPER_FADE_DURATION_MS via this.tweens.add, and sets spawnInProgress for KEEPER_INPUT_SUSPEND_MS via this.time.delayedCall. update() suspends on spawnInProgress (mirrors transitionInProgress). renderNpcs refactored to delegate to spawnNpcSprite (shared by initial render + post-spawn). cleanupResize iterates and invokes every spawnConditionUnsubscribe. NpcInteractionSystem and moveWithCollision now receive activeNpcs (reference-shared with GameScene so post-spawn pushes auto-propagate). (4) src/data/areas/fog-marsh.ts: appended Keeper NPC at (col 14, row 8) with sprite 'heron', wanderRadius 0, awarenessRadius 2, spawnCondition 'marsh_trapped == true AND escape_attempts >= 4 AND keeper_met == false' (third clause is the one-shot guard — phase-level criterion folded in here). (5) AGENTS.md: added 'Conditional NPC spawn' entry under Behavior rules covering the filter/subscriber/idempotency/input-suspend semantics. Verify: npx tsc --noEmit && npm run build pass; cd tools/editor && npm run build passes (NpcDefinition.spawnCondition is type-passive in the editor — unknown-but-acceptable optional). Regression check: PASS — diff vs docs/plan/test-baseline.txt shows only the build-hash + bundle-size change (1542 KB → 1544 KB; +1.7 KB from the new GameScene logic + types). Golden Principles: Loop-invariant EP-01 ✓ (subscriber per-flag-change not per-frame; spawnInProgress is a single boolean check; tween handed to Phaser); Conditional exits and decorations ✓ (same evaluateCondition + onFlagChange API — one shared mechanism); Trigger flag side-effects: unchanged; Save / resume: unchanged; Zone-level mutual exclusion ✓ (spawnInProgress mirrors transitionInProgress; suspends movement, NPC interaction, trigger eval, awareness all via update() early-return); NPC sprite rendering registry-driven ✓ (heron via NPC_SPRITES from US-70, no GameScene per-NPC code); Investigate-first ✓ (preceded by task 5). Quality checks: no-silent-pass N/A; no-bare-except N/A; error-path-coverage ✓ (unknown sprite -> rectangle fallback; malformed condition -> evaluateCondition returns false; null-sprite skip on registerNpc); agents-consistency ✓ (AGENTS.md row added). Scope check: 5 files all under PROJECT_ROOT ✓. Secrets scan: clean. Story Completion Gate: ALL [US-71]-tagged criteria met (12 + class baseline + idempotency + error path + AGENTS.md row + named constants). Ticked checkboxes; added US-71 to completed_stories. The combined criterion 'Keeper appears at (14,8) when condition true' tick was edited to reflect the third clause 'AND keeper_met == false' (phase-level one-shot guard folded into the spawnCondition rather than tracking already-spawned externally — author choice per spec)."
    result: pass
    notes: US-71 milestone complete; story commit fd5bb9e; AGENTS.md row landed in same commit so US-71's [US-71, phase]-tagged AGENTS criterion ticks here; phase-level Keeper-one-shot-guard criterion folded into spawnCondition third clause
    tokens: ~12000
  - task: 7
    time: "2026-04-26T10:35:00Z"
    type: investigate
    story: phase
    description: "Branch B — picking next task. US-70 + US-71 shipped; story milestones 2fe573e + fd5bb9e. Unmet criteria: US-72 (9 + error path + 2 regression), US-73 (12 incl. 2 AGENTS.md rows + tag [US-73, phase]), phase-level (full-arc manual verify, save/resume parity, reset parity, manual-verify checklist, atlas verification, AGENTS.md sweep, deploy verify), 2 safety criteria. Priority: US-72 next — provides the dialogue mechanism that lets the player interact with the spawned Keeper, and the flag flips that close the trap (marsh_trapped: false) and grant the ember (has_ember_mark: true). US-73 then closes the loop (overlay + path re-open via Phase 1 mechanism). Cached server/schema/cross-cutting classification — investigate-first mandated. Queued [US-72] investigate to confirm DialogueSystem.showNode injection point + endStoryScene exposure mechanism + keeper-intro/ember-given content authoring."
    result: pass
    notes: US-72 next; investigate-first enforced
    tokens: ~2500
  - task: 8
    time: "2026-04-26T10:42:00Z"
    type: investigate
    story: US-72
    description: "Branch A — executed queued [US-72] investigate. Source review: systems/dialogue.ts (678 lines): showNode (line 278) is called from start (line 197) and from advance/branch (lines 271, 497) — every node display routes here. Right injection point for setFlags: at the very TOP of showNode after `this.currentNode = node` and before redrawBox/applyPortrait/typewriter — fires BEFORE the typewriter starts so a downstream onFlagChange subscriber registered before dialogue.start sees the new value within the same call stack. Need to import setFlag from triggers/flags. close() (line ~640+) destroys UI then calls onEndCallback (line 659) — endStoryScene needs to be readable AT onEndCallback time, BEFORE this.script = null fires. Add private accessor getEndStoryScene() that returns this.script?.endStoryScene — but cache the value on close() before nulling, OR expose script.endStoryScene as a separate field that survives close. Cleaner: add an 'endStoryScene: string | null' field and capture it from the script in start(); reset in close(). GameScene.setOnEnd (line 205): existing callback runs exitDialogue + flushSave; we add `const endStoryScene = this.dialogueSystem.getEndStoryScene(); if (endStoryScene) this.launchStoryScene(endStoryScene);` AFTER flushSave (per spec criterion order). The existing launchStoryScene already errors gracefully when the id is unknown (line 636 'Story scene definition not found') — error path satisfied. fog-marsh story scene shape (line 380): { id, beats: [{ text, imageColor, imageLabel }] } — straightforward content authoring. Diff plan finalised."
    result: pass
    notes: showNode top-of-method injection point for setFlags; endStoryScene captured at start() into a private field (survives close); GameScene callback adds launchStoryScene call AFTER flushSave
    tokens: ~5000
  - task: 9
    time: "2026-04-26T10:55:00Z"
    type: implement
    story: US-72
    description: "Branch A — implemented US-72 per the diff plan from task 8. (1) src/data/areas/types.ts: DialogueNode gains optional setFlags?: Record<string, string|number|boolean> with inline doc-comment about the BEFORE-typewriter ordering and same-call-stack onFlagChange contract; DialogueScript gains optional endStoryScene?: string with inline doc-comment about the AFTER-flushSave launch order. (2) src/systems/dialogue.ts: imported setFlag from triggers/flags; added private endStoryScene: string|null = null field with inline doc-comment about close() lifecycle (this.script nulled before onEndCallback fires, so the value must survive in a separate field); start() captures script.endStoryScene ?? null; showNode fires node.setFlags at the top of the method (before clearChoices/redrawBox/typewriter); added public getEndStoryScene(): string | null reader; close() resets endStoryScene to null AFTER onEndCallback fires so getEndStoryScene returns the correct value during the callback. (3) src/scenes/GameScene.ts: setOnEnd callback now calls flushSave first then reads getEndStoryScene() and calls launchStoryScene when set — flushSave order preserved per spec criterion. (4) src/data/areas/fog-marsh.ts: appended 'keeper-intro' dialogue script (portraitId 'heron', endStoryScene 'ember-given', two nodes greeting->action, action node setFlags { has_ember_mark: true, keeper_met: true, marsh_trapped: false }) and 'ember-given' story scene (three beats: warm gold 0xd9a657 'The Keeper draws near' -> brighter ember 0xf2c878 'The ember passes' -> pale dawn 0xe8d8b8 'The fog parts'). Verify: npx tsc --noEmit && npm run build pass; cd tools/editor && npm run build pass. Regression check: PASS — diff vs test-baseline.txt shows only build-hash + bundle-size change (1544 KB -> 1545 KB; +1.3 KB from setFlags branch + endStoryScene field + new content). Golden Principles: Loop-invariant EP-01 ✓ (setFlags fires only on showNode, not per-frame); Trigger flag side-effects parity ✓ (DialogueNode.setFlags mirrors DialogueChoice.setFlags shape; same authoring vocabulary); Save / resume flush order ✓ (flushSave runs BEFORE launchStoryScene in setOnEnd); Conditional exits and decorations ✓ (marsh_trapped: false flip feeds the existing US-67/US-68 onFlagChange subscriber unchanged — collision/decoration restore for free); Investigate-first ✓ (preceded by task 8); error-path-coverage ✓ (existing launchStoryScene error path covers unknown endStoryScene id; existing scripts without endStoryScene field skip launchStoryScene via if-guard). Quality checks: no-silent-pass N/A; no-bare-except N/A; agents-consistency: no AGENTS.md change required for US-72 — the existing 'Trigger flag side-effects' Behavior rule covers DialogueChoice.setFlags pattern; DialogueNode.setFlags + DialogueScript.endStoryScene are mentioned only in the phase-level AGENTS.md sweep (deferred). Scope check: 4 files all under PROJECT_ROOT ✓. Secrets scan: clean. Story Completion Gate: ALL [US-72] criteria met (9 + error path + 2 regression). Ticked checkboxes; added US-72 to completed_stories. The marsh_trapped: false flag flip in the action node atomically reverses Phase 1's collision/decoration mechanism — US-73's path-re-opens criterion is now source-readable too (covered when US-73 implements the ember overlay)."
    result: pass
    notes: US-72 milestone complete; story commit d7ea45f; phase 'AGENTS.md sweep' criterion deferred to phase-level reconciliation
    tokens: ~10000
  - task: 10
    time: "2026-04-26T11:05:00Z"
    type: investigate
    story: phase
    description: "Branch B — picking next task. US-70 + US-71 + US-72 shipped; story milestones 2fe573e + fd5bb9e + d7ea45f. Remaining work: US-73 (12 criteria incl. 2 [US-73, phase] AGENTS.md rows), phase-level (full-arc manual verify, save/resume parity, reset parity, manual-verify checklist, atlas verification, AGENTS.md sweep, build pass, deploy verify), 2 safety criteria. US-73 is the last story — small in scope (ember overlay sprite anchored to player, flag-driven create/destroy, persist across area transitions and reload). Path-re-opens criterion is already source-true post-US-72 — the marsh_trapped: false flip feeds the existing US-67/68 onFlagChange subscriber. Investigate-first mandated. Queued [US-73] investigate."
    result: pass
    notes: US-73 next; investigate-first enforced
    tokens: ~2000
  - task: 11
    time: "2026-04-26T11:12:00Z"
    type: investigate
    story: US-73
    description: "Branch A — executed queued [US-73] investigate. Ember overlay rendering plan: Phaser.GameObjects.Arc (this.add.circle(x, y, radius, color)) — small warm-gold disc 6px radius, color 0xf2c878 (matches ember-given beat 2). setDepth(5.5) literal fractional depth between Entities (5) and Thoughts (8). Anchor: above player head — EMBER_OFFSET_Y = -28 (player sprite scaled 1.0, 68×68 PNG; -28 puts the ember just above the head silhouette without z-fighting). Per-frame update path: SINGLE setPosition call — `if (this.emberOverlay) this.emberOverlay.setPosition(this.player.x, this.player.y + EMBER_OFFSET_Y);` placed at the end of update() AFTER maybeAutosave. Zero allocations (Learning EP-01 — grep validation: no new keyword, no { ... } literals). Subscriber lifecycle: in create(), check getFlag('has_ember_mark') and create overlay if truthy (covers Continue-from-save and area-transition cases — scene.restart re-runs create). Subscribe onFlagChange('has_ember_mark') with hasEmberMarkUnsubscribe field; callback creates overlay when value is true (idempotent — null-check before this.add.circle), destroys when value is undefined or false (Reset Progress notifies subscribers with undefined per resetAllFlags semantics). cleanupResize: invoke unsub + destroy overlay. Reset path test: resetAllFlags wipes flagStore + notifies every subscribed name; the has_ember_mark subscriber receives undefined and destroys the overlay on the same frame. AGENTS.md updates: depth-map gets a row at 5.5 ('Player ember overlay'); Behavior rules gets a 'Player ember overlay' entry. UI camera ignore: world-space objects created after camera setup must call uiCam.ignore — but the overlay is created during scene.create() AFTER setupCamera() (in the post-camera lifecycle). Need to add the overlay to uiCam.ignore — but uiCam is in the cameras manager, and adding a sprite to it after construction requires this.cameras.getCamera('ui').ignore(overlay). Actually looking at line 496, ignore is set during setupCamera with the existing world objects. New world objects post-camera need explicit uiCam.ignore — TBD if Phaser handles this automatically or not. Will use this.cameras.getCamera('ui')?.ignore(this.emberOverlay) on creation. Diff plan finalised."
    result: pass
    notes: ember overlay design ready (Phaser.GameObjects.Arc 0xf2c878 6px); EMBER_OFFSET_Y = -28; subscriber lifecycle with idempotent create + destroy on undefined/false; uiCam.ignore on creation
    tokens: ~3500
  - task: 12
    time: "2026-04-26T11:25:00Z"
    type: implement
    story: US-73
    description: "Branch A — implemented US-73 per the diff plan from task 11. (1) src/scenes/GameScene.ts: EMBER_OFFSET_Y = -28, EMBER_RADIUS = 6, EMBER_COLOR = 0xf2c878, EMBER_DEPTH = 5.5 declared as named constants alongside KEEPER_FADE_DURATION_MS (Learning #6 facet — no inline magic numbers). New private fields emberOverlay (Phaser.GameObjects.Arc | null) + hasEmberMarkUnsubscribe (() => void | null). New private maybeCreateEmberOverlay() — idempotent (returns early if emberOverlay is non-null), creates this.add.circle at player position with EMBER_RADIUS/EMBER_COLOR, sets depth EMBER_DEPTH, calls cameras.getCamera('ui')?.ignore(overlay) so the UI camera doesn't double-render. New private destroyEmberOverlay() — null-safe destroy + null reset. In create() at end (after events.on('resume')): check getFlag('has_ember_mark') === true and call maybeCreateEmberOverlay; subscribe onFlagChange('has_ember_mark', (_, value) => { if (value === true) maybeCreateEmberOverlay(); else destroyEmberOverlay(); }) — handles both flag-set-true (create) and flag-undefined / false (destroy on Reset Progress). In update() at end (after maybeAutosave): single conditional setPosition call — `if (this.emberOverlay) this.emberOverlay.setPosition(this.player.x, this.player.y + EMBER_OFFSET_Y);` — zero allocations, zero object literals (Learning EP-01 audit pass). cleanupResize: invoke hasEmberMarkUnsubscribe + destroyEmberOverlay. (2) AGENTS.md: Depth map gains 'Player ember overlay' row at depth 5.5 between Entities and Thoughts. Behavior rules gains 'Player ember overlay' entry covering creation lifecycle, loop-invariant constraint, ui-camera ignore, and persistence (transition + reload via existing flag-store contract). Verify: npx tsc --noEmit && npm run build pass; cd tools/editor && npm run build pass. Regression check: PASS — diff vs test-baseline.txt shows only build-hash + bundle-size change (1545 KB -> 1546 KB; +0.8 KB). Golden Principles: Loop-invariant EP-01 ✓ (single setPosition; no per-frame allocations; per-frame branch is a single conditional); Conditional exits and decorations ✓ (overlay subscriber follows the existing onFlagChange pattern); Save / resume ✓ (relies on existing flag-store localStorage; no new persistence layer); Zone-level mutual exclusion ✓ (overlay update sits inside the world-walk branch of update() so dialogue/transition/spawn early-returns naturally suspend the position update); Investigate-first ✓ (preceded by task 11). Quality checks: no-silent-pass N/A; no-bare-except N/A; error-path-coverage ✓ (null-safe destroy via optional chain; idempotent create-guard); agents-consistency ✓ (AGENTS.md depth map + Behavior rules updated). Scope check: 2 files (src/scenes/GameScene.ts + AGENTS.md) under PROJECT_ROOT ✓. Secrets scan: clean. Story Completion Gate: ALL [US-73] criteria met (12 incl. 2 [US-73, phase] AGENTS.md rows). Ticked checkboxes; added US-73 to completed_stories. ALL FOUR STORIES US-70, US-71, US-72, US-73 NOW COMPLETE."
    result: pass
    notes: US-73 milestone complete; story commit 70c2410; all four stories shipped; remaining work is phase-level reconciliation (manual-verify doc + AGENTS.md sweep) + phase completion gate + PR + review
    tokens: ~7500
  - task: 13
    time: "2026-04-26T11:30:00Z"
    type: investigate
    story: phase
    description: "Branch B — picking next task. All four stories shipped; remaining 11 unchecked criteria are phase-tagged: 1 manual-verify doc creation, 1 AGENTS.md sweep, 1 build-pass (already pass), 1 atlas-frame-pick (N/A — circle, not atlas frame), 1 deploy verify (post-merge), 6 manual checklist scenarios documented in the manual-verify doc, 2 safety criteria (source-true on inspection). Combined docs reconciliation task: create the manual-verify doc + supplement AGENTS.md Dialogue rule with the new schema fields. Queued."
    result: pass
    notes: docs reconciliation queued as a single combined task
    tokens: ~1500
