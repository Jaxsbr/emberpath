phase: "npc-behavior"
started: "2026-04-24T11:33:03Z"
entries:
  - task: 1
    time: "2026-04-24T11:33:03Z"
    type: investigate
    story: phase
    description: "Branch B — phase start. Read AGENTS.md, phase-goal.md, spec at docs/product/phases/npc-behavior.md. Verify passes (tsc --noEmit && vite build). Asset staging exists at /Users/jacobusbrink/Jaxs/assets/emberpath/npc/{marsh-hermit,old-man}/{idle,walk,static}/ (directories present). Phase classified server/cross-cutting (new module src/systems/npcBehavior.ts + src/systems/npcSprites.ts, NpcDefinition field additions in types.ts, collision.ts + npcInteraction.ts must consume live positions, AnimationSystem's octant extracted to a shared location). Per the investigate-first mandate, queued an investigate task (not implement) to (a) enumerate staged asset files exactly and confirm naming convention, (b) audit NpcDefinition consumers and draft types.ts diff, (c) mirror fox-pip preload pattern for the NPC sprite registry, (d) Learning #70 octant consumer audit. Next iteration will execute this investigate and queue the first implement task."
    result: pass
    notes: phase classified server/cross-cutting; investigate-first enforced
    tokens: ~15000
  - task: 2
    time: "2026-04-24T11:40:00Z"
    type: investigate
    story: US-52
    description: "Executed queued investigate. Findings — (1) Asset staging verified: /Users/jacobusbrink/Jaxs/assets/emberpath/npc/marsh-hermit/ and /old-man/ each contain idle/<8 dirs>/frame_000..003.png (32), walk/<8 dirs>/frame_000..003.png (32), static/<dir>.png × 8 — matches spec exactly. Total 144 files, 72 per NPC. (2) NpcDefinition consumers in src/: types.ts (decl + AreaDefinition.npcs), areas/{ashen-isle,fog-marsh}.ts (definition sites), GameScene.ts:281-283 (the Rectangle render path to remove), collision.ts (getNpcBounds uses npc.col/row — must switch to live positions), npcInteraction.ts:70-71 (uses npc.col/row for nearest-NPC range — must switch to live positions), debugOverlay.ts:136-137 (uses npc.col/row for radius circles — spawn tile is correct for wander circle; interaction circle may want live position). Editor mapRenderer.ts:240-269 reads npc.col/row/color unchanged (editor operates on static defs). movement.ts has indirect NpcDefinition[] typing only. (3) Direction/octant audit (Learning #70): only ONE octant site in src/ — animation.ts:65 `velocityToDirection(vx, vy)`. GameScene:25 declares a pure DIRECTIONS list constant (no derivation). No other Math.atan2 or octant tables anywhere in src/. Extraction plan: move velocityToDirection to src/systems/direction.ts as pure exported function, have AnimationSystem import it, NpcBehaviorSystem import it. Single-owner extraction, clean. (4) Fox-pip preload pattern (GameScene.ts:50-57, 412-423): iterates DIRECTIONS × FRAME_COUNTS[anim], this.load.image per frame key `fox-pip-<anim>-<dir>-<i>`; then registerAnimations() creates `fox-pip-<anim>-<dir>` Phaser animations at 8 fps repeat:-1. NPC mirror: iterate registry entries, same pattern with `npc-<spriteId>-<anim>-<dir>-<i>` keys and `npc-<spriteId>-<anim>-<dir>` anims, plus static loaded as plain image keys `npc-<spriteId>-static-<dir>`. Queued the asset-copy task as the next unit (smallest commit; no TS changes)."
    result: pass
    notes: investigate-only — no code changes; verify state unchanged
    tokens: ~7000
  - task: 3
    time: "2026-04-24T11:45:00Z"
    type: implement
    story: US-52
    description: "Branch A — copied NPC sprite assets from staging to assets/npc/. cp -R each NPC subtree. File count verified per dir: marsh-hermit idle=32, walk=32, static=8; old-man idle=32, walk=32, static=8; 144 total. Verify passes (tsc --noEmit && vite build). Promoted baseline. No source changes yet — registry, types, and preload wiring come in the next iteration. 4 US-52 structural asset criteria now met (idle/walk/static trees exist for both NPCs); story still far from complete."
    result: pass
    notes: staging-only; US-52 asset-existence criteria met (4 of many)
    tokens: ~2000
  - task: 4
    time: "2026-04-24T11:48:00Z"
    type: investigate
    story: US-52
    description: "Branch B — prior investigate already produced full consumer map. Queuing the smallest next compile-together concern: extend NpcDefinition with sprite/wanderRadius/awarenessRadius and fill values in both area data files. Required fields mean types.ts + area files must ship together. Editor's mapRenderer reads only col/row/color, both retained, so editor build is unaffected."
    result: pass
    tokens: ~1000
  - task: 5
    time: "2026-04-24T11:55:00Z"
    type: implement
    story: US-52
    description: "Branch A — NpcDefinition extended with required fields sprite/wanderRadius/awarenessRadius; color retained with comment noting editor map-overview dependency. ashen-isle old-man and fog-marsh marsh-hermit updated with matching sprite ids and radii (2, 3). Verify tsc+build passes. US-52 structural criteria met: NpcDefinition fields ✓, color retained ✓, ashen-isle sprite/radii ✓, fog-marsh sprite/radii ✓."
    result: pass
    notes: 4 more US-52 structural criteria met
    tokens: ~3000
  - task: 6
    time: "2026-04-24T11:58:00Z"
    type: investigate
    story: US-52
    description: "Branch B — queue the NPC sprite registry module addition as the next concern. Pure data module, no consumers; matches fox-pip's DIRECTIONS+FRAME_COUNTS pattern but generalised to multi-sprite."
    result: pass
    tokens: ~500
  - task: 7
    time: "2026-04-24T12:02:00Z"
    type: implement
    story: US-52
    description: "Branch A — created src/systems/npcSprites.ts with NPC_DIRECTIONS, NPC_SPRITES registry (marsh-hermit & old-man at idle:4, walk:4), hasNpcSprite, getNpcSpriteIds. Verify tsc+build passes. Registry criterion met."
    result: pass
    notes: sprite registry scaffold in place; GameScene preload wiring next
    tokens: ~2000
# Each entry: {task, time, story, description, result, notes, tokens}
